apiVersion: apps/v1
kind: Deployment
metadata:
  name: impv-a-dfe
  labels:
    {{- include "falcon-controller.labels" . | nindent 4 }}
spec:
  {{- if not .Values.dfe.autoscaling.enabled }}
  replicas: {{ .Values.dfe.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "falcon-controller.dfe.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "falcon-controller.dfe.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.dfe.podSecurityContext | nindent 8 }}
      containers:
        - name: dfe
          securityContext:
            {{- toYaml .Values.dfe.securityContext | nindent 12 }}
          image: "{{ .Values.dfe.image.repository }}:{{ .Values.dfe.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.dfe.image.pullPolicy }}
          ports:
          {{ toYaml .Values.dfe.ports | indent 10 }}
          {{- if .Values.dfe.env }}
          env:
          {{- range $k, $v := .Values.dfe.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
          {{- end }}
            - name: DEPLOYMENT_PROFILE
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: deployment-profile
            - name: CONFIG_PUBLISHER_HOST
              value: http://cloud-connector
            - name: CONTROLLER_ID
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: controller-id
            - name: CONTROLLER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key:  controller-token
            - name: CONTROLLER_INSTANCE
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: controller-instance
            - name: ACCOUNT_ID
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: account-id
          {{- end }}
          resources:
            {{- toYaml .Values.dfe.resources | nindent 12 }}
      {{- with .Values.dfe.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.dfe.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.dfe.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: impv-a-summarization-engine
  labels:
    {{- include "falcon-controller.labels" . | nindent 4 }}
spec:
  {{- if not .Values.summarizationEngine.autoscaling.enabled }}
  replicas: {{ .Values.summarizationEngine.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "falcon-controller.summarizationEngine.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "falcon-controller.summarizationEngine.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.summarizationEngine.podSecurityContext | nindent 8 }}
      containers:
        - name: summarization-engine
          securityContext:
            {{- toYaml .Values.summarizationEngine.securityContext | nindent 12 }}
          image: "{{ .Values.summarizationEngine.image.repository }}:{{ .Values.summarizationEngine.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.summarizationEngine.image.pullPolicy }}
          ports:
          {{ toYaml .Values.summarizationEngine.ports | indent 10 }}
          {{- if .Values.summarizationEngine.env }}
          env:
          {{- range $k, $v := .Values.summarizationEngine.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
          {{- end }}
            - name: DEPLOYMENT_PROFILE
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: deployment-profile
            - name: CONTROLLER_ID
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: controller-id
            - name: CONTROLLER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key:  controller-token
            - name: CONTROLLER_INSTANCE
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: controller-instance
            - name: ACCOUNT_ID
              valueFrom:
                secretKeyRef:
                  name: controller-secret
                  key: account-id
          {{- end }}
          resources:
            {{- toYaml .Values.summarizationEngine.resources | nindent 12 }}
      {{- with .Values.summarizationEngine.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.summarizationEngine.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.summarizationEngine.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
